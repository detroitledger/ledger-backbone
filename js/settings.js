/*jslint nomen: true */
/*globals define: true */

define([
],

function(L) {
  'use strict';

  var settings = {};

  settings.api = {
    baseurl: '/api', // http://localhost:3000/api',
    geo: '/api'
  };

  return settings;
});
